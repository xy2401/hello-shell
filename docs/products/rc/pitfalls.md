# rc (Plan 9) 常见坑位

> 本页结论：rc 太过纯粹，导致习惯了 Bash 各种语法糖的用户会觉得“功能缺失”。

## 1. 缺乏强大的内置文本处理
rc 是一个纯粹的解释器。Bash 里的 `${var%suffix}` 等字符串截取魔法在 rc 里是不存在的。
**解法**：老老实实调用外部的 `sed`、`awk` 或 `grep`。

## 2. 只有标准的 Plan 9 生态才最舒服
在传统的 Linux/GNU 生态中使用 rc 会有些水土不服，因为很多 GNU 工具的选项设计默认是面向 Bash 用户的。
