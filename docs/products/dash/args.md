# Dash 参数与交互

> 本页结论：Dash 完全摒弃了交互式功能（如 Tab 补全、语法高亮、历史记录）。

## 纯粹的脚本执行器

Dash 被设计为 `/bin/sh` 的后端。它的唯一任务就是以最快的速度把系统启动脚本（init scripts）跑完。你永远不应该把 Dash 设置为你终端登录的默认交互 Shell。

- 使用 `$1`, `$2`, `$@` 解析参数。
- 必须使用 `getopts`（而不是 Bash 的长参数或者高级解析库）来处理选项。
