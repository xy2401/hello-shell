# Dash 语法与特性

> 本页结论：Dash 的语法完全受限于 POSIX 规范，它没有任何超出标准的“语法糖”。

## 极致的 POSIX 标准

Bash 支持很多扩展（如 `[[ ]]`, `数组`, `大括号展开 {1..10}`）。在 Dash 中，这些**全部不存在**。

```sh
# 错误：Dash 不认识 [[
if [[ "$a" == "$b" ]]; then ...

# 正确：必须使用标准的单中括号 test 命令
if [ "$a" = "$b" ]; then ...
```
