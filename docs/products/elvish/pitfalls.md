# Elvish 常见坑位

> 本页结论：Elvish 最大的学习门槛在于它彻底打破了传统 Unix Shell 的一些语法直觉。

## 1. 变量引用的差异
在 Bash 中直接使用 `$VAR`，但在 Elvish 中，输出命令并不能像 Bash 那样隐式拼接。
**坑位**：习惯了 Bash 的用户可能会写出 `echo "Hello $name"`，但在 Elvish 中这可能不奏效，或者需要特定的语法拼接。

## 2. 闭包与作用域
Elvish 大量依赖匿名闭包（`{|x| ... }`）。如果不理解函数式编程的作用域隔离，很容易在循环累加等场景下陷入作用域的陷阱。
