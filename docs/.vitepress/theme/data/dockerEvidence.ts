const snapshots=import.meta.glob('../../../../demos/*/docker/*.out.txt',{query:'?raw',eager:true})
export function dockerEvidence(product:string,kind:'inventory'|'session'|'assert'|'native.inventory'|'native.session'|'native.assert'){const value=snapshots[`../../../../demos/${product}/docker/${kind}.out.txt`] as string|{default?:string}|undefined;return typeof value==='string'?value:value?.default||''}
