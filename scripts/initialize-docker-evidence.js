#!/usr/bin/env node
import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const products=['bash','cmd','fish','powershell','zsh'];for(const id of products){const dir=path.join(root,'demos',id,'docker');fs.mkdirSync(dir,{recursive:true});for(const kind of['inventory','session','assert']){const file=path.join(dir,`${kind}.out.txt`);if(fs.existsSync(file))continue;const native=id==='cmd';const body=kind==='inventory'?`PENDING: ${id} 的 builtins 与 PATH executable 清单尚未由 ${native?'Windows runner':'Docker'} 采集。`:kind==='session'?`PENDING: ${id} 工具会话尚未采集；现有 demos/${id}/00..08 快照仍是行为证据。`:'RESULT: not executed — partial catalog entry, not full tooling evidence.';fs.writeFileSync(file,`---\nstatus: documented\ncapturedAt: null\ndockerImage: "${native?'native-windows-runner':'unresolved'}"\nexitCode: -1\n---\n${body}\n`)}}console.log(`Initialized ${products.length} shell tooling evidence directories`)
