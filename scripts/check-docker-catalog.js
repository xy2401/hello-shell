#!/usr/bin/env node
import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const expected=['bash','cmd','fish','powershell','zsh'];const source=fs.readFileSync(path.join(root,'docs/.vitepress/theme/data/dockerCatalog.ts'),'utf8');const ids=[...source.matchAll(/\bid:\s*['"]([^'"]+)['"]/g)].map(m=>m[1]);const failures=[];for(const declaration of['DockerImageRef','DockerCatalogEntry','DockerEvidenceStatus'])if(!source.includes(`interface ${declaration}`)&&!source.includes(`type ${declaration}`))failures.push(`missing unified type ${declaration}`);for(const id of expected)if(!ids.includes(id))failures.push(`catalog missing ${id}`);if(new Set(ids).size!==ids.length)failures.push('duplicate catalog id');const env=fs.readFileSync(path.join(root,'.env.versions'),'utf8');for(const line of env.split(/\r?\n/)){if(!/^[A-Z][A-Z0-9_]+_IMAGE=/.test(line))continue;if(!line.includes('@sha256:'))failures.push(`unpinned image: ${line.split('=')[0]}`)}for(const id of expected)for(const kind of['inventory','session','assert']){const file=path.join(root,'demos',id,'docker',`${kind}.out.txt`);if(!fs.existsSync(file))failures.push(`missing ${id}/docker/${kind}.out.txt`)}if(!fs.existsSync(path.join(root,'docs/matrix/docker-tools.md')))failures.push('missing Docker matrix');if(failures.length){console.error(failures.join('\n'));process.exit(1)}console.log('[docker-catalog] 5 shell products covered; Windows native exceptions preserved');
